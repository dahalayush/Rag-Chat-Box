services:
  rag-app:
    build: .
    container_name: rag-chatbot
    restart: unless-stopped

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - .:/app

    command: uvicorn main:app --host 0.0.0.0 --port 8000
